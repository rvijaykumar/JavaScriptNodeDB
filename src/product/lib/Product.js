// const Utils = require('../../common/utils/Utils');
const logger = require('../../common/utils/Logger');


module.exports = {

    //Get Products
    async get(event) {
       logger.info(event);
    },

    // Create Product
    async create(event) {
        logger.info(event);
    }
 };
